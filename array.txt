For Array problems
  1.Check wheather it'll be an advantage to sort the array,if so do it.
  2.Try to apply two pointers approach.(Ref : Two Sum II,Three Sum Problem on LeetCode).3
  3.Choose between using a set or length(Set will automatically sort the array and remove all the
duplicates).
---------------------------------------------------------------------------------------
USE OF HASHMAPS IN ARRAY PROBLEMS : 
	1.Longest subarray with equal numbers of 0's and 1's.
	2.Longest subarray with Zero Sum.
	3.Longest substring without repeating characters. 